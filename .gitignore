# Generated analysis outputs - regenerated by running the notebook against
# live Wind data, not source. Kept on disk for caching (see get_fund_nav_data /
# get_index_daily_returns), just not version-controlled.
*.csv
*.png
